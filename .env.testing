APP_ENV=testing
APP_DEBUG=true
APP_URL=http://localhost

# Use in-memory SQLite for tests
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Cache, queue, and session configurations
CACHE_DRIVER=array
QUEUE_CONNECTION=sync
SESSION_DRIVER=array

# Filesystem configurations
FILESYSTEM_DISK=public
